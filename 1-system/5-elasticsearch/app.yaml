spec:
  name: elasticsearch-ingestion
  rolloutStep: infrastructure
  autoSync: true
  source:
    repoURL: https://github.com/Unique-AG/hpe-hosted-trial
    targetRevision: feat/upgrade-to-2026
    path: "1-system/5-elasticsearch"
    directory:
      recurse: true
      include: '*.yaml'
      exclude: '{app.yaml}'
