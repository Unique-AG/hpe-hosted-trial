apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: kong-id-virtual-service
spec:
  gateways:
    - istio-system/ezaf-gateway
  hosts:
    # HPE: id.ingress.pcai0201.fr2.hpecolo.net
    - id.2.28.18.215.sslip.io
  http:
    - match:
        - uri:
            prefix: /
      rewrite:
        uri: /
      route:
        - destination:
            host: zitadel.unique.svc.cluster.local
            port:
              number: 8080
          headers:
            request:
              set:
                X-Forwarded-Proto: https
