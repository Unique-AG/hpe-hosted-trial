apiVersion: v1
kind: Secret
metadata:
  name: litellm
type: Opaque
stringData:
  PROXY_MASTER_KEY: "{{ .proxyMasterKey }}"
  LITELLM_SALT_KEY: "{{ .saltKey }}"
  TOGETHERAI_API_KEY: "{{ .togetherAiApiKey }}"
  NVIDIA_EMBEDDING_KEY: "{{ .nvidiaEmbeddingKey }}"
  NVIDIA_LLM_KEY: "{{ .nvidiaLlmKey }}"